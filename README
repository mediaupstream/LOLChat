### LOLChat

This is a super simple NodeJS chatroom demo, with a twist: It translates your messages into LOLCat speak!  

Take a look at `server.js` to see what's going on. I wouldn't recommend doing it this way but it serves as an example of what can be done with NodeJS in such little code.  

### Basic usage

`$ node server.js`

Now point you and your friends browsers to `localhost:5000` and start the LOLz